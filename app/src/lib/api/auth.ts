// Client API Auth Helpers
// Most auth operations are managed directly via Supabase client inside authStore.ts

export async function checkAuthStatus() {
  // Helper for checking auth session
  return true;
}
